{{ form_start(form) }}
<div class="row">
    <div class="col-md-6">
        <div class="box box-primary">
            <div class="box-header with-border">
                <h3 class="box-title">Identificador</h3>
            </div>
            <div class="box-body">
                {{ form_widget(form) }}
            </div>
            <div class="box-footer">
                <button class="btn btn-primary">{{ button_label|default('Guardar') }}</button>
            </div>

        </div>
    </div>
</div>
{{ form_end(form) }}
