{% extends 'base.html.twig' %}

{% block title %}Tema{% endblock %}

{% block body %}
    <h1>Tema</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Titulo</th>
                <td>{{ tema.titulo }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ tema.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('temas_index') }}">back to list</a>

    <a href="{{ path('temas_edit', {'id': tema.id}) }}">edit</a>

    {{ include('temas/_delete_form.html.twig') }}
{% endblock %}
